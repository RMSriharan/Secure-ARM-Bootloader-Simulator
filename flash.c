// flash.c
#include "flash.h"
#include <stdio.h>

void load_kernel(const char *filename) {
    puts("Kernel loaded to memory (simulated).");
}

void update_flash(const char *filename) {
    puts("Flash update simulated.");
}
